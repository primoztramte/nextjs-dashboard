# Use your current image as the base
FROM mcr.microsoft.com/devcontainers/typescript-node:1-22-bookworm

# Install the PostgreSQL client permanently
RUN apt-get update && apt-get install -y postgresql-client
